# Host Codex: prepare, then repair WU-05 in the same session

Use the exact `K0_PACKAGE_SHA` supplied in the external publication receipt/paste-ready prompt. Do not infer it from a moving branch.

You are explicitly authorized as HOST_CODEX_ORCHESTRATOR to run the pinned bootstrap script below; a separate human merge is NOT required. This narrow permission supersedes earlier blanket prohibitions on Codex branch preparation. Do not manually reset, rebase, stash, amend, force-update or resolve conflicts.

Preserved branch: `research/k0-stage-telemetry-integration-20260827`.
Preserved review: `e95ce1e58a603306cb665a6ab91cfe02d279972f`.
Preserved tree: `e3621a370297a76907e97730ebd18c5c1e0fb83e`.
Keep WU-00–04, the review commit and raw campaign bytes.

Locate `/tmp/vigilode-k0-stage-telemetry.kAguIL/tree`; if moved, use read-only `git -C ~/vigilode worktree list --porcelain`. Follow the exact shell sequence in `WU05_BOOTSTRAP_V2_HANDOFF.md`, extracting it from the pinned Git commit if not yet present locally. Never run a package-only validator from the unprepared implementation tree.

The runner validates actual package dependencies in a detached worktree, checks complete structured markers and pinned file inventory, rejects non-control source changes, preserves inherited review-side validator repairs, and makes only the ordered `[review, package]` merge. An exact clean existing merge is revalidated without another commit. No reset occurs after post-merge failure. Capture `BOOTSTRAP_RECEIPT=...`.

Only after `LOCAL_WU05_AUTHORITY_READY`, switch to WU-05 IMPLEMENTER. Read `WU05_LOCAL_CODEX_PROMPT.md`, its seven contracts, and preserved `fresh_review_findings.yaml`. Do not restart WU-00–04. Repair only the five original findings plus already-bound supplement cases. Use only the two authorized doc-hidden K0 public bridges. Preserve Cargo manifests, equations, tolerances, production signatures/routes and homotopy certification.

Generate evidence-v3 wrappers mechanically from immutable raw WU-04 data. Do not invent historical signed-residual evidence or claim that packaging tests are scientific validation. Reuse campaign evidence only with explicit current-source binding; a changed packaging SHA alone is not a rerun reason.

Execute real targeted negative/mutation tests, source-derived bridge audit, evidence checks, the existing single read-only fresh repair review, and final differential audit. Keep pre-existing P3 findings nonblocking. Do not publish the source branch/implementation PR before P0/P1 closure. Then open only the draft stacked implementation PR; no merge, activation, timing, ranking, tag or release.

Return actual command results, package/prepared/final commit/tree, five finding dispositions, raw evidence preservation, skipped work and exact reasons, and GitHub/Jira PM-7/Confluence 15499267 readbacks. Use ATLAS_SYNC_PENDING on an actual integration outage. Do not ask questions or guess across a genuinely unresolved scientific boundary.
