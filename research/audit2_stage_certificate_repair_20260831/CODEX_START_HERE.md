# Local Codex job: bounded stage-certificate repair closeout

Operate as `LOCAL_CODEX_JOB_ONLY`; no local LLM. This is
`USER_AUTHORIZED_SUCCESSOR_OBSERVED_DEFECT_CLOSURE`, not a reopening or fresh
reconstruction. Read this directory; `EXECUTION_CONTRACT.json` is the sole
machine authority. Validate the exact open Draft PR stacked on #41 first.

## R0 — bind and resume

Keep the predecessor run
`/home/cosmosapjw/.local/state/vigilode/stage-certificate-telemetry/20260831T141627Z`
immutable. Verify its manifest and SHA256SUMS hashes. Resume
`LATEST_VALID_DURABLE_CHECKPOINT`: derive the preserved dirty-worktree path
from `terminal/command_readback.json`, then create a fresh successor run and
atomically checkpoint HEAD/tree, porcelain-v2-z status, tracked/staged binary
diffs, and SHA-256/size/content copies of every allowed untracked path.

Never reset, clean, stash, rebase, overwrite, or checkout across that dirty
worktree. Create a clean worktree at this successor PR head. Replay only
checkpointed stage-certificate implementation/test/formal/evidence paths.
Require byte identity for non-overlaps and explicit three-way semantic merge
with base/before/after hashes for overlaps. Stop on unexplained dirty paths.

Verify the two harness archive hashes and apply their evidence-first research
contract, scientific contract, validation matrix, failure log, and independent
review rules. Do not copy the archives into Git.

## R1 — math/physics research loop

Freeze `FORMAL_SCOPE.md`, then close exactly
`F01_QUANTIFIER_SCOPE_INSUFFICIENT`,
`F03_QUANTIFIER_SCOPE_INSUFFICIENT`, and
`F04_QUANTIFIER_SCOPE_INSUFFICIENT`.

Lean/mathlib and Rocq are universal authorities. Their exported theorems and
external consumers bind arbitrary finite n>=1; fixed Fin 3 or numerical fixtures
cannot discharge F01/F03/F04. Require both finite-Neumann inverse directions,
distinguish RHS b from rho=b-Wx, and prove arbitrary-stage componentwise and
nonnegative weighted bounds.

Compile generic probes; bind theorem signatures; run Lean #print axioms, Rocq
Print Assumptions and coqchk; reject sorry, Admitted, project-local axioms,
missing evidence, or role escalation. Require exactly 13 formal role records.
Wolfram, Sage, and Singular remain limited exact cross-checks. Sage uses
predeclared rational dimensions including n!=3; Singular is F02 numerator-only.
xAct is outside scope. F02/F05 are regression-only. Add the real-to-binary64
directed-bound bridge.

## R2 — coding research loop

Write RED tests, then the smallest repair. `max_arnoldi` is a
`TOTAL_ARNOLDI_VECTOR_CAP` for each completed GMRES solve row, independent of
iteration_limit; restart is per-cycle. Use LinearSolveReport.iterations, or an
explicit arnoldi_vectors field if trace iterations means something else. Never
use residual-history length. Equality is legal; max+1 rejects even below
iteration_limit; one bad completed row rejects admission.

Add a concrete serde_json receipt serialize/deserialize round trip into the same
type, structural equality, canonical reserialization, and preservation of all
hashes/provenance. Implement correctly upward-rounded nonnegative add/multiply:
exact +0 and exact-representable values stay exact; inexact values bump only
when needed. Product/sum overflow gives +infinity, then typed nonfinite rejection
with no decision. These P2 tests are closure tests, not new gates.

No candidate, real-client, historical controller/example, or holdout execution.

## R3 — integrated verification

Run all 13 formal roles/probes, focused Rust tests, candidate-free readiness,
default/no-default/feature checks, clippy -D warnings, fmt, diff check, and
compact publication audit. Record exact argv/cwd/exits/counts/hashes externally.
Old checks cannot validate repaired bytes. Only one diagnostic retry is allowed
for the successor node. Extend existing observed-failure orchestration; do not
add a standalone gate.

## R4 — one review

Freeze one source snapshot and perform exactly one fresh review event producing
`integrated_closeout_review.json` with software_contract and formal_scope
lenses. Zero P0/P1 is required. Any P0/P1 terminates STOP_INVALID; no repair or
second review follows.

## R5 — compact publish/readback

Commit only allowed source/tests/proofs and compact analysis. Raw logs, compiler
products, caches, archives, datasets, and dependencies stay external. Use BYTE,
CONTENT, NUMERICAL, and SEMANTIC identity only with declared comparators. Never
use byte equality as theorem or numerical proof.

Push only to this branch/PR; no merge/tag/release, other PR, Jira, or Confluence.
Two process-only cycles terminate `STALL_PROCESS_ACCRETION`; identity/plan
drift terminates `PROCESS_DRIFT_DETECTED`. Preserve honest failure receipts.

```text
local_llm_used: false
candidate_executions: 0
holdout_access: NOT_OPENED_OR_EXECUTED
raw_data_committed: false
claim_ceiling: EXPLORATORY_NONAUTHORITATIVE_REUSABLE_PRECONDITIONER_TRANSACTIONAL_STEP_SUBSTRATE
```
