# Quantified formal repair scope

This candidate-free algebra lane cannot raise the project claim ceiling. Let
n>=1 and use the canonical unscaled Euclidean L2 schema.

## F01

For arbitrary strict-lower T indexed by Fin n, prove T^n=0 and, for
S=sum(k=0..n-1,T^k), both (I-T)S=I and S(I-T)=I. Fixed 3x3 is supplemental only.

## F03

For arbitrary two-sided inverses W,V, arbitrary b,x, rho=b-Wx, and kappa>=0
with norm(V v)<=kappa norm(v) for every v, prove Vb=x+Vrho,
norm(Vb-x)<=kappa norm(rho), and
norm(Vb)<=norm(x)+kappa norm(rho). Kappa bounds the inverse operator norm; b and
rho are distinct.

## F04

For arbitrary nonnegative strict-lower A and q>=0, define
z_i=q_i+sum(j<i,A_ij z_j). Prove z>=0, its finite-Neumann representation,
componentwise domination of any admissible d, and alpha.d<=alpha.z,
beta.d<=beta.z for arbitrary nonnegative endpoint/estimator weights.

F02 stays the frozen operator Jacobi numerator/square-zero regression. Its fixed
fixture is allowed only there and cannot supply F03. F05 stays the general safe
interval/threshold implication; the undecided gap is typed inconclusive.

Lean/mathlib and Rocq export the arbitrary-n theorem ABI and compile external
generic consumers. Bind source, ABI, probe, theorem signature, assumption audit,
and output hashes. Run Lean #print axioms, Rocq Print Assumptions and coqchk; no
sorry, Admitted, or project-local axiom.

Exactly 13 unique records are required: F01 Lean/Rocq; F02 Wolfram/Sage plus
Singular numerator-only; F03 Lean/Rocq plus Wolfram scalar cross-check; F04
Lean/Rocq plus Sage generated-dimension cross-check; F05 Lean/Rocq. CAS backends
never become universal authorities. Exit zero alone is insufficient.

Wolfram planning checks of the scalar geometric series and estimator interval
were exploratory cross-checks only. Rump (DOI 10.1587/NOLTA.6.360) and Kellison
et al. (DOI 10.1109/Correctness56720.2022.00007) are background, not authority.
