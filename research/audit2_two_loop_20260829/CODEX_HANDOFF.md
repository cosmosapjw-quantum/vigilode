# Codex continuation: audit2 science/code, not old K0 bootstrap

Start from `research/audit2-output-policy-20260829` in `cosmosapjw-quantum/vigilode`. Its inspected scientific base is `codex/vigilode-scientific-validity-v2@93fe348ce36859dd5f78b31267d771ea9c054677`. The final publication receipt/PR supplies the actual delivery head. Do not run any old K0 START_CONTINUATION, merge its package, reset the user's existing worktrees, or reopen sealed holdouts.

## Completed continuation work

The branch was inspected in a new isolated worktree without resetting the user's existing worktrees. The research branch is a direct two-commit descendant of the stated scientific base; the differing publication SHA is a provenance rebind, not evidence of archive corruption.

Exactly one fresh-context review of the pre-repair bounded diff returned `REQUEST_CHANGES`. It confirmed residual sign and no production activation, and identified decimal coefficient leakage, missing uncertainty authority, erased failure work, zero-RHS 0/0, and formatting. The dispositions are recorded in `evidence/fresh_review_disposition.md`.

The repair adds explicit `ReferenceUncertaintyTreatment`; a result-independent `64*f64::EPSILON` projection for the separate research target; typed attempt/completion/partial-progress accounting; mass/nonnormal/zero/failure tests; and a non-default `audit2-research` feature. `FullTargetOracle` remains the default research backend and `CommonWBlockForward` requires opt-in. Both comparison arms use matching trial stage states. Historical gates, production coefficients/residual/routing, the54 records, and sealed holdouts are unchanged.

## Completed scoped verification and delivery binding

The final aggregate structured-correction suite passed all 11 tests in one invocation. The bounded affected Rust suites passed 49 tests, the coefficient-adjacency checks passed 2 tests, and the independent Python contracts passed 8 tests. The analysis script re-read all 54 compact records and recomputed the six saved full-trajectory metrics. Formatting, affected clippy checks, the no-default-feature build, JSON/CSV parsing, the three historical SHA-256 checks, and the immutable historical subtree diff all passed. See `evidence/continuation_verification.log` and `evidence/verification.json`.

The earlier full-workspace attempt remains `NOT_RUN`/not verified for this continuation because runtime replacement interrupted it. It is not relabelled as a PASS and no large campaign was launched.

The actual delivery HEAD/tree, live CI state, and Atlassian synchronization are external-state facts. They are supplied by the final OPEN/DRAFT/UNMERGED PR receipt and existing PM-1/companion-page comments, not self-recorded inside the commit they identify. PM-7/K0 remains incomplete and untouched.

## Reproduction, not mandatory duplicate work

From the repo root:

```bash
python3 tools/audit2_output_policy_research.py --output /tmp/vigilode-audit2-fresh
cargo test --locked -p rodas5p-fair-ab --test output_accuracy_assessment_contracts
cargo test --locked -p rodas5p-integrators --features audit2-research --test audit2_structured_correction_contracts -- --nocapture --test-threads=1
cargo test --locked -p rodas5p-integrators --test dense_output_v2_contracts
cargo test --locked -p rodas5p-integrators --test homotopy_numerical_contracts
```

Normal Cargo access suffices; the optional external offline vendor is not a prerequisite. Use the actual tests recorded here instead of blindly rerunning a large campaign. A full workspace attempt was interrupted by runtime replacement; it is NOT a PASS. The final aggregate 11-test structured suite recorded in the continuation log is the controlling scoped result.

## Closure and handback

Return exact changed source paths, current base/head/tree, actual test logs, review dispositions, unchanged historical-input checks, condition/domain counterexamples, and the scoped research PR. Separate RESULT_VALIDITY, PROVENANCE_VALIDITY, and PACKAGING_VALIDITY. The claim ceiling is `EXPLORATORY_NONAUTHORITATIVE` research diagnostic only: no unprojected exactness, nonlinear certificate, accuracy PASS, timing/ranking/speedup, BDF/CVODE, production, holdout/freeze, or scientific-publication admission. No new bootstrap, schema, manifest family or review tier. No fabrication of missing historical evidence.
