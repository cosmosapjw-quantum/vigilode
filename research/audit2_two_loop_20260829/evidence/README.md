# Executed evidence

`final_affected.log` is the actual restored-source pre-continuation Cargo output:40 tests across5 suites. It is not relabelled as verification of the continuation edits. `python_tests.log` is the actual8-test Python output. `mutation_excerpts.md` quotes the three actual failing mutation runs and binds the full logs by SHA-256; full logs, all54-row diagnostics, refinement data and plots are in the downloadable research bundle. The full data are reproducible with the directly tracked `tools/audit2_output_policy_research.py` and original repository evidence, without any external bundle prerequisite.

`fresh_review_disposition.md` records the one fresh-context `REQUEST_CHANGES` review and the repaired dispositions. `continuation_verification.log` records the final scoped commands, exit statuses, counts, immutable-input hashes, and deliberate omissions. `../data/continuation_identity_raw.csv` retains the 12 per-`(n,h)` observations emitted by the final structured test; 19 emitted numeric/work fields were mechanically compared with a fresh `--nocapture` run, and the remaining independent-apply count is a direct test invariant. `../data/continuation_correction_summary.csv` retains compact condition, backward-error, state, nonlinear-residual, domain-counterexample, and work-accounting observations without fabricating unavailable per-case fields. `verification.json` separates inherited evidence from the final continuation verification.

The source SHA-256 values in `verification.json` identify the working-tree source bytes at the document snapshot. They are source-provenance checks, not equality criteria for numerical backends. JSON report formatting and archive compression need not be bit-identical.

No full-workspace completion or second fresh-context review is claimed. Actual final HEAD/tree, CI result, and workstream synchronization are live external receipts rather than self-referential fields inside the delivery commit. No old K0 bootstrap is required. The claim ceiling remains `EXPLORATORY_NONAUTHORITATIVE` projected-target research diagnostic only.
