# Plot-driven hostile audit

## Verdict

`PASS`

Both figures are deterministic, byte-reproducible renderings of the verified summary.

- The wall figure preserves all 35 measured pairs, labels the logarithmic axis and arm order,
  shows each seven-pair median, and visibly exposes rather than suppresses the N=384 host-noise
  regime.
- The ledger figure correctly stacks retained-prefix and continuation JVP work over committed
  R-JF JVP and labels the 0.799–1.194% profile totals.
- The underlying JSON/CSV and both PNG files reproduced byte-for-byte from the checked scripts.
- No measured pair is excluded.

Neither figure authorizes a speedup, fresh-safety, or active-switching claim.
