# Next node recommendation

Do not activate switching from v3.6.

The next admissible node is a precommitted timing-replication and continuation-transaction gate:

1. preserve the complete v3.6 campaign, including the noisy N=384 pairs;
2. freeze host-quality diagnostics and exclusion rules before a new wall measurement;
3. repeat complete alternating-order campaigns rather than selecting individual favorable pairs;
4. decide whether continuation requires a separate transaction using the already measured
   prefix-only and total-speculative ledgers;
5. commit a genuinely fresh shadow-safety coverage/profile contract before its first solver
   output and before any safety promotion;
6. keep R-JF as the sole committed trajectory and keep N=2048 sealed.

No active speedup, switching, or release-wide completeness claim is available at this boundary.
