# v3.2 Plot CRAG Audit

## Correctness

`ZETA34_VS_FULL_E_ERROR.png` shows all five unsafe events at moderately/high zeta34, but also substantial safe overlap. This supports ranking information, not deterministic separation.

`ZETA23_ZETA34_PHASE_GEOMETRY.png` shows near-collinearity in log-space between zeta23 and zeta34. The relative-drift normalization removes absolute scale and performs poorly, consistent with the complete AUC audit.

## Retrieval

Exponential Rosenbrock theory motivates analyzing nonlinear remainder vectors generated by dynamic linearization. It does not supply a theorem that this particular quadratic-cancelled WRMS drift is a local-error certificate. Therefore the empirical gate remains authoritative for promotion.

## Augmented checks

- N=96 and N=256 independently satisfy the predeclared AUC floor for zeta34.
- unsafe orientation agrees across dimensions.
- every leave-one-family-out zeta34 AUC remains above the predeclared 0.60 floor; minimum 0.6875.
- prior controls kappa234 and chi34 do not satisfy the same robustness combination.
- safe/unsafe zeta34 ranges overlap, preventing post-hoc deterministic threshold claims.

## Generation

The correct next experiment is not another discovery transformation. It is independent calibration of the already-selected feature form `zeta34` on the previously sealed N=192 profile, with threshold-selection and multiplicity/risk rules committed before any N=192 solver output.

## Claim status

SURVIVING: zeta34 as a discovery-stage safety-ranking witness.
REJECTED: relative_drift as an authority witness.
FORBIDDEN: discovery-fitted zeta34 threshold; active switching.
