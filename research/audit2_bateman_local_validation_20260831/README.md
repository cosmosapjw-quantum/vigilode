# Exact Bateman local validation control package

This directory is the amended pre-execution control package for one guarded
execution of the exact Bateman six-case client. Candidate executions remain
exactly zero. The first published controller invocation stopped in a sealed
candidate-free Rust preflight because its implementation-origin checkout
lacked two compile-time documents; no marker or candidate launch occurred.

## Start here

- `PREREGISTRATION.md` freezes identities, rules, mathematical scope, and the
  tri-state evidence boundary.
- `CODEX_START_HERE.md` is the `HOST_CODEX_ONLY` local execution and
  candidate-free adjudication prompt.
- `handoff.json` is the machine-readable handoff.
- `MATHEMATICAL_SCOPE.md` and `MATH_BLOCKER_LEDGER.json` bind the pre-result
  non-coding blocker analysis.
- `evidence/VERIFICATION.md` records candidate-free implementation and
  publication verification, plus the preserved initial preflight failure.

Published controller C1 `b5f553b5be24598c71bc7af15c97e67f503610a5`
has tree `7e96da6f612eade18acc77b700b6e847d68160c9` and is contained in
Draft PR #40, stacked directly on Draft PR #39. The controller must not run
until PR #40's final documentation head is read back and all required checks
are green.

C1 fixes the runner and adjudicator; the C2 documentation binding changes no
C1 script byte. The controller chooses a per-run canonical build target and
the adjudicator closes candidate-free artifacts without trusting package-recorded
Git or Python executables.

The repaired controller binds the executable checkout to PR #39 head
`6b00a886c4eb38d3fe199e3d77852cc1eb35eb39` while separately retaining its
direct parent `cac7d1b7337a6dff25a60072009658f6ddf155d9` as the scientific
implementation origin. Their delta is documentation-only; the one-shot key,
scientific bytes, cases, budgets, and admission rules are unchanged.

The manifest IEEE-754 bits are authoritative over ledger decimal rendering.
An adjudicated `ACCEPT` means only that the six frozen contracts passed, not
that the omitted `Ehat + Theta`, `sum_i a_i q_i`, and per-solve GMRES evidence
was certified.

The project claim ceiling remains
`EXPLORATORY_NONAUTHORITATIVE_REUSABLE_PRECONDITIONER_TRANSACTIONAL_STEP_SUBSTRATE`.
