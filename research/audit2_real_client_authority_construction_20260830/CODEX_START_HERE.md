# Superseded local-execution prompt

The authority-construction phase remains candidate-free. Its earlier manual
execution recipe is superseded because it did not provide a canonical
account-global one-shot guard, exact command/event verification, or independent
tri-state adjudication.

For the only authorized Bateman local execution, use:

> `../audit2_bateman_local_validation_20260831/CODEX_START_HERE.md`

That prompt binds repaired published controller commit
`b5f553b5be24598c71bc7af15c97e67f503610a5`, tree
`7e96da6f612eade18acc77b700b6e847d68160c9`, and Draft PR #40. Do not
manually run, splice, or reproduce the candidate command from historical text.

The executable source checkout is the PR #39 documentation head
`6b00a886c4eb38d3fe199e3d77852cc1eb35eb39`; its direct parent
`cac7d1b7337a6dff25a60072009658f6ddf155d9` remains the scientific
implementation origin and the one-shot identity.

For regression binding only, the legacy candidate-command prefix was
`cargo run --locked -p rodas5p-integrators --features audit2-bateman-authority`.
This deliberately incomplete prefix is not execution authorization; the only
authorized complete command is fixed inside the published controller above.

Candidate executions at this handoff remain exactly zero. The authority
manifest, references, budgets, scenarios, feature-gated scientific source,
holdout boundary, and claim ceiling are unchanged.

Any later Bateman `ACCEPT` is limited to the six frozen receipt contracts; it
does not supply the stronger `Ehat + Theta`, `sum_i a_i q_i`, or per-solve
GMRES certificate. Manifest IEEE-754 bits remain authoritative over ledger
decimal rendering.
