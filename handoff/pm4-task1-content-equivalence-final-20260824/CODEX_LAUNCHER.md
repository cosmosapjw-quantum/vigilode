# Codex launcher

Start Codex in `~/vigilode` and give it this instruction:

```text
Fetch or locate branch
handoff/pm4-task1-content-equivalence-final-20260824.

Create a detached read-only handoff worktree if needed. Read its AGENTS.md and
execute IMPLEMENTER_PROMPT.md completely.

Do not ask me to run terminal commands. Inspect the machine and repository,
choose mechanically verified path/toolchain/vendor substitutions, and perform
the local verification and ordinary feature push yourself.

Do not use outer archive checksums as a blocker. Use Git source authority,
content/diff equivalence, Cargo frozen closure, and scientific/code tests.

Stop after publishing and verifying PR #11. Do not merge or start Task 2.
```

After success, open a new Codex context and give it `FRESH_REVIEW_PROMPT.md`.
